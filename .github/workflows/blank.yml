name: clean anofm data
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: run jmeter on github action
        uses: sebiboga/run-jmeter-github-action@v0.0.5
          
